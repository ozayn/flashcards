# Public API URL (for display; client calls go via /api/proxy)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-only: Railway private networking URL. When set, backend calls use this instead of NEXT_PUBLIC_API_URL.
# API_INTERNAL_URL=
