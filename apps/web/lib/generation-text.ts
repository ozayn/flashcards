/**
 * Max characters for AI text-mode generation (Create Deck Text tab, deck detail “Add more cards” Text).
 * Must stay in sync with `TEXT_MAX_LENGTH` in `apps/api/app/api/generation.py`.
 */
export const GENERATION_TEXT_MAX_CHARS = 50_000;
