# =============================================================================
# Railway Deployment - Environment Variables
# =============================================================================
# Set these in Railway Dashboard: Project > Service > Variables
# =============================================================================

# -----------------------------------------------------------------------------
# API Service (apps/api)
# -----------------------------------------------------------------------------
# Required: PostgreSQL connection. Add Postgres plugin to get this automatically.
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: For future LLM features (flashcard generation, etc.)
# OPENAI_API_KEY=sk-...

# -----------------------------------------------------------------------------
# Web Service (apps/web)
# -----------------------------------------------------------------------------
# Required: Must point to your deployed API URL.
# Use Railway's private domain for same-project: http://api.railway.internal:8000
# Or public URL: https://your-api.up.railway.app
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# Note: NEXT_PUBLIC_* must be set at build time. Railway injects them during build.
